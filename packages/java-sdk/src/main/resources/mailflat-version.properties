# Single source of truth: filled from pom.xml at build time (resource filtering).
# Never edit the value by hand. B-052: four packages once reported a version that
# differed from the one actually published, because it was written in two places.
version=${project.version}
