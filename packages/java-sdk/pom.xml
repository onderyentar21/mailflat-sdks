<?xml version="1.0" encoding="UTF-8"?>
<!--
  MailFlat Java SDK — Maven build.
  Connected to: deploy/backup_db.sh yok; /api/v1 Agent API'sini sarar (Python/JS SDK ikizi).
  Coordinates: net.mailflat:mailflat-sdk. Runtime dep: Jackson (JSON). Test: JUnit 5 (+ JDK HttpServer).
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>net.mailflat</groupId>
  <artifactId>mailflat-sdk</artifactId>
  <version>0.2.0</version>
  <packaging>jar</packaging>

  <name>MailFlat Java SDK</name>
  <description>Official Java client for MailFlat — disposable, automation-friendly email inboxes with one-line OTP retrieval. Built for Selenium/JUnit test suites.</description>
  <url>https://mailflat.net</url>

  <licenses>
    <license>
      <name>MIT License</name>
      <url>https://opensource.org/licenses/MIT</url>
    </license>
  </licenses>

  <properties>
    <maven.compiler.release>11</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <jackson.version>2.17.2</jackson.version>
    <junit.version>5.10.3</junit.version>
  </properties>

  <dependencies>
    <!-- JSON — tek runtime bağımlılığı. HTTP için java.net.http (JDK gömülü, dep YOK). -->
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <version>${jackson.version}</version>
    </dependency>

    <!-- Test — JUnit 5. Sahte sunucu = JDK com.sun.net.httpserver (ek dep yok). -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
      </plugin>
    </plugins>
  </build>
</project>
